{
  "name": "sample-app",
  "version": "1.0.0",
  "description": "Sample application that demonstrates how to use admin-ui as a submodule",
  "main": "index.js",
  "scripts": {
    "dev": "cd server && npm run dev",
    "start": "cd server && npm start",
    "build": "npm run build:client",
    "build:client": "cd client && npm run build",
    "build:standalone": "cd server && node scripts/build-client.js",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "clean": "rm -rf node_modules server/node_modules client/node_modules package-lock.json server/package-lock.json client/package-lock.json"
  },
  "dependencies": {},
  "devDependencies": {}
} 
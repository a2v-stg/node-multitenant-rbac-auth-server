<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Fraud Detection Engine</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f3f8fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .vue-app-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>
    <div id="app" class="vue-app-container">
        <!-- Vue.js app will mount here -->
        <div class="text-center">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="mt-3">Loading Vue.js application...</p>
        </div>
    </div>

    <!-- Vue.js and dependencies -->
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script src="https://unpkg.com/vue-router@4"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    
    <!-- Your Vue app scripts -->
    <script src="/vue-legacy/client/dist/js/chunk-vendors.js"></script>
    <script src="/vue-legacy/client/dist/js/app.js"></script>
    
    <script>
        // Initialize Vue app with LoginRefactored component
        const { createApp } = Vue;
        const { createRouter, createWebHistory } = VueRouter;
        
        // Import your LoginRefactored component
        // This assumes your Vue app is properly configured
        const app = createApp({
            template: `
                <div>
                    <login-refactored></login-refactored>
                </div>
            `,
            components: {
                'login-refactored': window.LoginRefactored || {}
            }
        });
        
        app.mount('#app');
    </script>
</body>
</html> 